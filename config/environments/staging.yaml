---
# Staging Environment Configuration
environment: staging
#deploy_root: /usr/local/bin/ignition/data
gateway:
  url: http://localhost:8188
  username: munekamgilbert@gmail.com
 # container_name: ignition-staging
 # backup_path: ./backups/staging
database:
 # host: localhost
  #port: 5432
  #name: ignition_staging
  #username: ignition
projects:
  # List of projects to deploy (will be populated by CI/CD)
  # Format: project-name: PerspectiveWebPage: build/PerspectiveWebPage.zip
deployment:
  auto_backup: true
  backup_retention_days: 7
  #pre_deploy_scripts:
   # - db_migrate
  #post_deploy_scripts:
   # - smoke_test
